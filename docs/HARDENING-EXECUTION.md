# Nexus hardening — исполнительный чеклист (сверено с кодом 2026-07-27)

> **✅ ЗАВЕРШЕНО 2026-08-01.** Все батчи (A мелкие фиксы · B eslint · C тест-починка · D рефакторинг
> 6 god-файлов) закрыты; плюс §9А аддитивные сокеты Task. Проверка на каждом шаге: typecheck + api-тесты
> 60/60 + web-тесты 4/4 + прод-build. Остаётся: (1) **визуальная приёмка** 6 рефакторингов в браузере
> (сверено сборкой/типами/тестами, кликом — за Владом); (2) правильный baseline `_prisma_migrations`
> dev-БД (:5433) — низкий приоритет; (3) `.env` `TEST_DATABASE_URL` → :5434 (за Владом). Follow-up-долги:
> мёртвый код (ProjectDetail в pages/projects, `TrackFormModal`-импорт), полное переписывание DESIGN.md
> под kit.css, TOCTOU дубль клиента, оптимистичный DnD и пр. — в git-истории/аудите.

> Уточнённый список ПОСЛЕ сверки аудита с реальным кодом (аудит завышал долги). Родитель — [HARDENING-PLAN-2026-07-27.md](HARDENING-PLAN-2026-07-27.md), первопричины — [AUDIT-2026-07-27.md](AUDIT-2026-07-27.md). Работаем на ветке `knyazzer`.

## ✅ Сделано в этой сессии

- Phase 0 — влит хвост (форма назначений + чистка мёртвых RBAC-модулей) — `35d4a3c`
- Phase 1 безопасность — helmet · rate-limit на auth · CORS без LAN в проде · non-root контейнер — `92f6b58`
- Гигиена — isActive-обход lifecycle убран · дедуп genTempPassword · чистка неиспользуемых deps — `8c7ad71`
- Уже были в коде (аудит устарел): fail-fast JWT, fail-closed requireRole, field-level RBAC на финансы, events-синк, nginx-sync.

## 🔒 Отложено на инфру (не локально)

- Ротация `JWT_SECRET` + пароля БД (координация с Inventory) · переезд домена · донор cutover · отключение Google Sheets.

## Батч A — мелкие безопасные фиксы

- [ ] **№2 — изолировать политику удаления проекта.** Вынести «кто может удалить проект» в единую точку (`services/policy.ts` → `canDeleteProject`), пока = только админ (текущее поведение). Меняется в одном месте при детальной проработке RBAC. `DELETE /projects/:id` перевести с `requireRole('admin')` на `authenticate` + `canDeleteProject`.
- [ ] **№4 — суффикс табельного в placeholder-email штата.** У штата `generateEmail(name)` без дизамбигуации → тёзки тихо теряются при bulk-import. Добавить `.{tabNumber}`-суффикс, как у фрилансеров.
- [ ] **№11 — DESIGN.md: развести поколения токенов.** Устаревшие Urbanist/оранжевые таблицы вынести в архив-раздел/файл, в основном теле оставить только актуальные Figma v2 (Inter/индиго).
- [ ] **№3 — `@unique` на `tabNumber` + миграция (локально).** Схема + миграция, применить на dev. ⚠️ **Напоминание Владу:** перед прод-деплоем проверить дубли — `SELECT tabNumber, count(*) FROM nexus.users WHERE tabNumber IS NOT NULL GROUP BY tabNumber HAVING count(*)>1`. NULL допускает много значений в unique — ок.

## Батч B — инструменты

- [ ] **№6 — eslint для `apps/api`.** Конфиг + скрипт `lint`. Прогнать, тривиальное поправить, остальное — показать списком (не «чинить» массово молча).

## Батч C — Phase 3: тесты-страховка (перед рефакторингом)

- [ ] Починить устаревший `useAuth.test.ts` (замокать Supabase-сессию / `api`-интерцептор).
- [ ] Web-тесты на 6 god-страниц: рендер + ключевые взаимодействия — минимальная страховка от регрессий рефакторинга.

## Батч D — Phase 6: рефакторинг god-файлов (по одному, под тестами, визуальная сверка)

Чистый рефакторинг: поведение/вид без изменений; вынос под-компонентов, хуков состояния, чистых утилит; страница = тонкий дирижёр.

- [ ] TasksPage (1980) — первым (крупнейший, центральный)
- [ ] ChatsPage (1391)
- [ ] OrgChart (1316)
- [ ] ProjectsPage (1176)
- [ ] ProjectCardPage (1136)
- [ ] CalendarPage (1091)

## Порядок

A (мелкое) → B (eslint) → C (тесты) → D (рефакторинг). Каждый пункт — отдельный коммит, `typecheck` + api-тесты 60/60 зелёные (тест-URL передавать явно: `:5434/tvshifts_test`, пока `.env` не поправлен).
