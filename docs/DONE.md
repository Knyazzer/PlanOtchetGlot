# DONE — журнал выполненного

> Хронология крупных этапов (по git-истории, 446 коммитов с 2026-04-07). Только дополнять, не реструктурировать.

## 2026-04 — rebuild-v3: скелет и базовые модули
- Сброс до минимального скелета (`55efc8e`, ветка rebuild-v3): монорепо pnpm (api/web/db), вместе со старым кодом удалён тестовый набор (~163 теста прежней версии).
- Auth (JWT cookies), Users, тёмная/светлая тема, AppShell с навигацией через `useState<Page>`.
- Чат-модуль целиком: direct/self/support/group, WebSocket-хаб, unread-бейджи, реакции-заготовки, оптимистичные обновления, advisory locks против гонок.
- Календарь: события (meeting/task/personal) с участниками и авто-задачами, общие записи (Знаменки/HR) для админа.
- Задачи: Kanban + Gantt, журнал изменений (TaskLog), seen-уведомления, авто-статус из событий.

## 2026-05 — треки, проекты, персонал
- Треки: этапы (Stage), участники, привязка задач; вкладка в Задачах.
- Проекты: Client → Project → WorkItem (роли исп./лайн-продюсер, аккаунт-менеджер), вкладка Workflow (сводный реестр WI), отделы на WI (WorkItemDivision), финансы — бюджет + расходы по статьям (Expense).
- Персонал: штат и фрилансеры, импорт из Google Sheets (буфер сотрудников, реестр фрилансеров, КФПД → клиенты), оргсхема (OrgChart) + построение структуры из данных листов.
- Импersonation (вход под пользователем по ссылке), личный кабинет.

## 2026-06 — SSO, прод, интеграция с Инвентаризацией
- **Миграция авторизации на self-hosted Supabase Auth** (`20260609_sso_multischema`): мультисхема `public`/`nexus`, тонкая identity-таблица `public.users` (id = auth.uid), `nexus.users.auth_id`, удалён `passwordHash`; dev-login для локальной разработки.
- **Прод-деплой** `nexus.knzteam.ru`: CD (push в master → GitHub Actions → GHCR → SSH на VDS), Nginx Proxy Manager + Let's Encrypt, Grafana Alloy + Postgres Exporter.
- **SSO-портал**: `/login?redirect=` с whitelist-доменами, токены через URL-хеш, защита от петли редиректа (и от 414), единый логаут `/?logout=1` (`signOut scope: global`).
- **Онбординг**: «Выдать доступ» (временный пароль + форс-смена при первом входе), link-if-exists для уже зарегистрированных в auth.users, массовый онбординг (`bulk-onboard`), сброс пароля админом.
- **Жизненный цикл пользователя (Этап 1)**: уволен=архив — deactivate (бан GoTrue + `public.is_active=false` + снятие табельного) / reactivate; автогенерация табельных (S### штат, FL# фрилансер); показ уволенных; админы видны в списке (карточка read-only); понятный 409 при конфликте email; слияние дубля аккаунта супер-админа.
- **Интеграция с Инвентаризацией** (Этап 1 завершён): общий Supabase, схемы `public`/`nexus`/`inventory`, роли БД, SSO в оба приложения, единый логаут; контракт и план Этапа 2 — `docs/INTEGRATION.md`; общий репозиторий договорённостей `megapolis-platform`.
- **Дом-кит** (ветка `design`): снят запрет UI-библиотек, подключены Tailwind v4 + shadcn-токены + lucide; Figma-прототип Work OS как визуальный эталон; план переноса ПланОтчета — `docs/IMPLEMENTATION-PLAN.md`.

## 2026-06-11 — аудит
- Полный аудит проекта (/docsup): код, мёртвый код, тесты, документация — `docs/AUDIT-2026-06-11.md`. Документация актуализирована (CLAUDE.md, RULES.md, README.md, ACCOUNTS.md), создан план `docs/TODO.md`.

## 2026-06 — бета-доступ, списки, бренд
- **Бета-доступ в платформу** (`canAccessPlatform`) + столбец «Учётка» в персонале (состояния учётки, жёлтый кружок «обводка без заливки» = приглашён-не-активирован).
- **Раздел «Списки»** (заменил «Базы данных»): общие справочники + Клиенты + Форматы дня. API `/refs` (`RefList`/`RefItem`, 7 базовых списков) + тест.
- **Форматы дня — управление**: добавление/удаление форматов (active-флаг, умный DELETE), версионирование; вкладка в Настройках (убраны Структура/Синхронизация).
- **Системный аккаунт** без рабочего пространства (разделы Настроек в сайдбаре).
- Логотип Nexus по бренд-канону (текстура) — favicon + `NexusIcon`.

## 2026-07 — hardening и безопасность
- **Комплексный аудит Nexus + экосистемы** и план ремонтных работ — `docs/AUDIT-2026-07-27` / план hardening.
- **Безопасность API**: helmet + rate-limit на auth, CORS без LAN в проде, non-root контейнер; BAC-гарды (защита от broken access control), фиксы миграций, наладка стенда/портов.
- **Hardening lifecycle**: убран обход жизненного цикла через `PATCH isActive`; дедуп пароля; изоляция политики удаления проекта; суффикс табельного в email штата.
- **БД-гарантия**: `@unique` на `User.tabNumber` (вместо app-level).
- ESLint (flat config) + скрипт lint; отгорожены устаревшие токены дизайна (Urbanist/оранжевый) как архив.
- **Форма назначений** роли/места в оргструктуре (personnel) + чистка RBAC.

## 2026-08-01 — рефакторинг god-файлов, ui-kit AppShell, фундамент экосистемы
- **Разнесены god-файлы** по подпапкам: `TasksPage` (1980→172), `ChatsPage` (1391→822), `OrgChart` (1317→303), `ProjectsPage` (1176→86), `ProjectCardPage` (1136→115), `CalendarPage` (1092→278).
- **Внедрение ui-kit AppShell** (Этап 1, 6 задач): sync AppShell+tokens.css + шрифт Steppe, токен-мост (палитра кита + фиолетовый акцент #7B61FF), свап каркаса Nexus на китовый AppShell (610→414), `.sidebar-dark`-scoped preflight (1:1 с китом), блоки меню «Платформа»/«Внешние сервисы», лого «Нексус» кириллицей, колокол уведомлений в правый верх шапки.
- **`docs/ECOSYSTEM-AGGREGATION.md`** — фундамент «Nexus как слой сведения экосистемы»; аддитивные nullable-«розетки» на `Task` (§9) под будущую агрегацию.

## 2026-08-02 — интерактивность календаря + ui-kit sync
- **Календарь на кит-компонентах**: нативные date/time-инпуты → `DatePicker`/`TimePicker`; диапазон времени → `ClockDial` (тумблер 12/24 ч, дефолт 12 ч); чипы Дата/Время с мини-модалами сбоку от события; поповер «Календари» (поиск + выбрать/снять все) вместо сайдбара категорий; контролы вида/навигации — в шапку.
- Убран `backdrop-blur` со всех оверлеев (канон кита — только затемнение).
- **HeaderPortal** — инфраструктура телепортации контролов страниц в китовую шапку (Задачи/Команда/Аналитика/Календарь); слияние Свод в Аналитику как внутренней вкладки.
- Справочник PWA (`docs/PWA-DESKTOP-APP.md`).

## 2026-08-04 — Пульс (новости), кабинет-рабочий стол, график работы
- **Пульс = новостная лента** (`Post`, API `/posts` + модуль `adm.news`): лента + редактор поста; «Главная» → «Пульс»; 2 колонки (лента + рельс кто работает/дедлайны); лайтбокс изображений, Timeline-вид.
- **Кабинет сотрудника → рабочий стол**: под-вкладки [Обзор · Задачи · Треки] в левом меню; месячная сводка справа + дате-зависимый кабинет; виджет статуса.
- **График работы (HR)** (`/work-schedule`, часть A): недельный паттерн типов дня + часы → «тип дня по умолчанию» (подсказка, не факт).
- Календарь §5/§6: overlap параллельных событий, drag-перенос силуэтом между днями, боковая карточка без затемнения + живая обводка.
- Сэмпл-сид оргструктуры + штата для dev (5 департаментов, 9 сотрудников, гранты).

## 2026-08-05 — presence, «Мой рабочий день», производственный календарь
- **Presence** (`GET /work-schedule/presence`): единый статус присутствия штата на сегодня (DayEntry + график) — Пульс «кто работает»; счётчики часов, выходные.
- **«Мой рабочий день»**: форматы до 6, таймер, автосейв, баланс; кнопка всегда видна (дизейбл), самостоятельный статус; чипы отсутствия видны весь день.
- HR-доступ к справочнику форматов дня (`FormatsTab`); рабочая смена пароля в профиле (вместо заглушки).
- Пульс: производственный календарь месяца (вместо персональных часов) + квартал и обратный отсчёт до конца квартала.
- UI-типографика к канону (тело 14, заголовки 22, нижний порог 12px, токены в rem).

## 2026-08-06 — трек = чат, встречи внутри трека
- **Трек = чат** (§9): при создании трека авто-создаётся групповой чат (`Chat.trackId`, лидер=админ чата, участники автоподключаются); PUT members синкает состав чата; кнопка «Чат трека».
- **Встречи внутри трека** (§9): событие привязывается к треку (`Event.trackId`) и видно в его деталях.

## 2026-08-07 — производственный календарь Пульса, палитра ролей
- Пульс: производственный календарь с графиками (донат месяца + кварталы-бары), фирменный Tooltip, недельная полоса (общий календарь) с фильтрами «кто работает» по HR-статусам, перекомпоновка колонок, единая система elevation.
- **Семантические роли цвета** (вариант A, `lib/roleColors.ts`): цвет = функция кнопки/сущности; раскатка primary/highlight/success/danger; убраны оранжевые тинты выделения/чипов.
- Тёмная тема: увеличена контрастность лестницы поверхностей (исправлена инверсия elevation).
- `docs`: полная функциональная карта Nexus (18 модулей + «заложить сейчас» + 23 неочевидных).

## 2026-08-08 — Заявки, модель дня (место/статус), цели, Стратегия-канбан
- **Модуль «Заявки»** (отпуск/больничный/отгул, `Request`, `/requests` — спека `docs/REQUESTS-MODULE.md`): Этап 1 бэкенд (types/list mine|inbox/create/decision/cancel, резолв согласующего по оргструктуре) + тест; Этап 2 UI (под-вкладка в «Мой кабинет», оформление через DatePicker); Этап 3 генерация заявления **docx** (`services/requestDocx.ts` + `GET /requests/:id/document`). Badge-счётчики в меню (`/requests/unseen-count`), интеграция в колокольчик `/notifications`, отзыв (revoke) одобренной с уведомлением автора.
- **Модель дня — разделение места и статуса** (`docs/DAY-STATUS-MODEL.md`): `DayEntry.place` (office/remote/project/trip) ↔ `dayFormat` (working/weekend/vacation/sick/dayoff); миграция + data-миграция, 8 слоёв рефактора; разделены календарный выходной (работать можно) и отгул; ночные смены (переход через полночь) во всём бэке; **консистентность статусов** — одобрение заявки отражается в `DayEntry.status`, читается везде (кабинет/присутствие/свод/MonthStrip/календарь = один источник), отзыв откатывает.
- **Цели компании** (`CompanyGoal`, `/company-goals`) — микро-блок на Пульсе (админ правит). **Личные цели** (`PersonalGoal`, `/personal-goals`) — блок «Мои цели» в Обзоре кабинета (чекбокс достигнуто).
- **Стратегия — канбан-раскладка** страницы: 3 колонки План/Реализовано/Не реализовано с drag-drop (смена статуса, итог при «не реализовано»), переключатель департамента + под-вкладки отделов, блок «Зоны роста» (`kind=growth`), «Доработки к собранию» (`MeetingNote`, `/meeting-notes`).
- Кабинет/рабочий день: переработан «Мой рабочий день» (одна кнопка Начать→Закончить, поля по стадии, TimePicker, модель план vs факт с откатом «↺ По расписанию»); дизайн-модал подтверждения `useConfirm` (по правилу попапов, без блюра) вместо нативного confirm.

## 2026-08-09 — «Задачи на сегодня» (рабочий набор), Стратегия Фазы 2/3, метрики
- **Стратегические цели Фаза 1** (`StrategicGoal` + `Task.goalId`/`Track.goalId`, `/strategic-goals`, `StrategyPage` — спека `docs/STRATEGIC-GOALS.md`): цели квартал/год, каскад департамент→отдел, CRUD + close (итог), RBAC director/head/admin. **Фаза 2**: закрытие периода, переносы, прошлые периоды read-only. **Фаза 3**: история изменений (`StrategicGoalLog`, `GET /:id/log`), привязки задач/треков к цели, прогресс (`tasksTotal/tasksDone/trackCount` + roll-up департамента), уведомления о подключениях к трекам, оргроли.
- **Разграничение видимости Стратегии**: `scope=cabinet` (Обзор/кабинет) = только своя область даже у дир/рук; страница «Стратегия» = сотрудник свой департамент, рук/дир — вся компания.
- **«Задачи на сегодня» = рабочий набор дня** (спека `docs/superpowers/specs/2026-08-09-today-tasks-working-set-design.md`): POST принимает status, PATCH ставит `startDate`=сегодня при взятии в работу; список = день + статус {inprogress, done}, чекбокс done↔inprogress; ручной порядок drag за grip (`TaskDayOrder`); Обзор в две колонки; смарт-«Связь» (один сгруппированный список Треки/Стратегические цели/Проекты с иконками типа).
- **Row-drag на @dnd-kit** (паттерн support/ServersView): активная строка едет за курсором, соседние раздвигаются, reorder оптимистичный через `setQueryData`; серия фиксов плавности дропа.
- **Глобальные тосты** сверху по центру (`lib/toast.ts` + ToastHost) — добавлено/создано/отправлено/сохранено/удалено.
- **Метрики = сухие факты** без формул; Аналитика и Свод объединены в одну страницу (`docs/DECISION-2026-08-09-metrics-and-backend.md`).
- **Блок B**: ACID-транзакции вокруг реальных многошаговых записей.
- Редизайн карточки задачи (Создатель/Исполнитель в строку, единая «Связь», TimePicker вместо ручного ввода минут); переназначение задачи сбрасывает её в Бэклог.

## 2026-08-10/11 — модель «окно» дедлайнов, инфопанель, история навигации
- **Модель «окно» дедлайнов** `[startDate, deadline]` (Влад снял возражения): факт считается один раз в день закрытия (незакрытые задачи → в день закрытия), порядок по дням; drag-на-день (без дедлайна — переезд, с дедлайном — подтверждение переноса).
- **Инфопанель дедлайнов в Обзоре** — сводка «N с дедлайнами / M просрочено» с переходом в задачу.
- **История навигации браузера** — кнопки назад/вперёд листают страницы (History API pushState/popstate, без React Router, URL не трогаем ради SSO-хеша).
- **MonthStrip** — счётчики задач и закрытие дня; метка рабочего дня = место, статусы по dayFormat.
- Дебаг-кейс «отпуск-сирота» — `docs/POSTMORTEM-2026-08-10`.

## 2026-08-27/28 — предрелизная проверка БД, фиксы прода
- **Ранбук предрелизной проверки БД** перед крупным деплоем (`docs/DEPLOY-DB-PRECHECK-2026-08-27.md`): бэкап → read-only проверки прода → генеральная репетиция `migrate deploy` на клоне.
- Доска задач для оси агентов (BACKLOG/TODO/DONE.md в корне, формат чекбоксов для парсера kanban.js).
- Фиксы прода: пин `pnpm@10.33.0` (pnpm 11 не читает `onlyBuiltDependencies`); `public.users.id` стал TEXT — убран `::uuid` (500 на увольнении/онбординге); принимать статус дня `working` в PUT day-entries (500); тип drag-handle из useSortable; календарь на светлой теме (разделители/подсветки), карточка дня (русская метка + плашка «нельзя закрыть»).
