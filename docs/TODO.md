# TODO — TV Shifts

Актуальный список задач. Завершённое — в [DONE.md](DONE.md).

---

## 🔴 Критично

> Нет критических багов.

---

## 🟠 Важно

### Автосинхронизация не работает
`server.ts` не содержит ни импорта `node-cron`, ни планировщика. Синхронизация запускается **только вручную** через `POST /sync/trigger`.  
DONE.md и CLAUDE.md утверждают, что cron реализован — это неверно.  
**Действие:** реализовать `cron.schedule('*/30 * * * *', () => runFullSync())` в `server.ts`, либо явно убрать автосинх из документации если это сделано намеренно.

---

## 🚧 В разработке (заглушки)

- [ ] **Вкладка "Задачи"** — `TasksPage.tsx` — бэкенд полностью готов (`/tasks/*`), нужен UI
- [ ] **Вкладка "Аналитика"** — `AnalyticsPage.tsx` — бэкенд готов (`/analytics/*`), нужен UI
- [ ] **Вкладка "Профиль"** — `ProfilePage.tsx` — бэкенд готов (`/users/:id`, `/shifts/monthly-summary`), нужен UI

---

## 📋 Функционал

### 🟠 Планировщик смены — пресет и синхронизация

Новая вкладка в карточке матрицы (`InternalShiftsPanel.tsx`) между «Команда» и «Производственные расходы». Дизайн задокументирован в `.superpowers/brainstorm/1183-1776628921/content/shift-planner-design-spec.md`.

- [ ] **Временные пресеты для ТВ/Телерадио** — Планировщик отображается для всех форматов и локаций. При открытии нового проекта с `project.format` ∈ {ТВ, Телерадио} блоки заполняются дефолтными временными диапазонами из хардкодного пресета. Для остальных форматов (Радио, Съёмки, Оффлайн, Менеджмент) пресета нет — блоки создаются пустыми.
- [ ] **Таймлайн-компонент** — CSS grid 4-cell (corner | headerScroll | namesScroll | tracksScroll), горизонтальный + вертикальный скролл только в `tracksScroll`, синхронизация JS scroll listener
- [ ] **Рендер блоков из `group_schedule`** — блоки (Сбор, Завоз, Монтаж, Эфир/копии, Демонтаж/копии, Вывоз) с абсолютными минутами, динамический диапазон (`min-60` … `max+60`), шаг 30 мин
- [ ] **Person-first строки** — одна строка на специалиста, тонкая линия-коннектор через все его блоки, greedy stacking при перекрытии
- [ ] **Drag-and-drop блоков** — перемещение целого блока + resize краёв; прямой DOM во время drag, `render()` только на pointerup
- [ ] **Попап даты блока** — клик (< 5px движения) → ◄ дата ► + кнопка «Закрыть»; шаг ±1440 мин
- [ ] **Резайз диапазона участника** — ручки на краях бара участника, зажаты диапазоном родительского блока, шаг 30 мин
- [ ] **«Начало эфира» — маркер-playhead** — вертикальная линия поверх timeline, перетаскивается в пределах Эфир-блока. Значение хранится в `group_schedule.efir.broadcastStart`. Двусторонняя синхронизация: тянем в Планировщике → обновляется поле в GroupDateBlock «Команда»; редактируем там → сдвигает playhead
- [ ] **Зум** — кнопки +/− (шаг ×1.3), диапазон 18%–600%; Ctrl+колесо с привязкой к курсору
- [ ] **Сохранение изменений** — PATCH `/status-rows/:id/group-schedule` для дат/времён блоков; отдельная схема для персональных диапазонов (решить: новый JSONB `shift_schedule` на `project_members` или расширить `field_approvals`)

---

### 🟠 Вызывной лист

Кнопка «Вызывной лист» внутри вкладки «Команда» карточки матрицы. Генерирует текстовый документ с составом смены.

- [ ] **Поле `telegram_username`** — добавить колонку в БД `project_members.telegram_username`; синхронизировать из Google Sheets колонки F (при условии, что там ник, а не телефон — логика: если значение начинается с `@`, брать как ник)
- [ ] **Редактирование `telegram_username` в UI** — инлайн-редактирование в TeamTable (аналогично другим JSONB-полям)
- [ ] **Генерация plain-текста вызывного листа** — формат: название проекта, дата, по каждой группе список «Имя — HH:MM–HH:MM»
- [ ] **Telegram-версия** — тот же текст с `@username` вместо имени там где ник известен; кнопка «Копировать для Telegram»

---

### 🟠 Вкладка «Фрилы» в карточке матрицы

Вкладка рядом с «Команда» внутри `InternalShiftsPanel`. Не-штатные участники проекта с отдельными полями оплаты.

- [ ] **Отдельный тип записи или флаг** — решить: `is_freelancer` на `project_members` или отдельная таблица `project_freelancers`; рекомендуется флаг на `project_members` (меньше дублирования схемы)
- [ ] **Поля оплаты** — `payment_type` (enum: наличные / карта / СБП / счёт), `payment_status` (enum: не оплачено / ожидает / оплачено), `payment_amount`
- [ ] **UI вкладки** — таблица фрилансеров с инлайн-редактированием полей оплаты, кнопка «+ Добавить фрила»
- [ ] **Кнопка «Запросить оплату»** — формирует запрос для глобальной страницы «Фрилансеры» (статус → «ожидает»)

---

### 📋 Глобальная страница «Фрилансеры»

Отдельная страница в основной навигации. Агрегированный вид всех фрилансеров по всем проектам.

- [ ] **Новый пункт навигации** «Фрилы» в `AppShell.tsx` (видимость: `admin | producer`)
- [ ] **`FreelancersPage.tsx`** — таблица: фрилансер × проект × статус оплаты. Фильтры: по месяцу, по статусу, по имени
- [ ] **API** — `GET /freelancers` с агрегацией по `project_members` (с фильтром `is_freelancer = true`); возможно объединить с `/project-members` через query param `?freelancers=true`
- [ ] **Запросы на оплату** — список строк со статусом «ожидает», возможность отметить «оплачено» массово

---

- [ ] **Diff UI конфликтов** — панель разрешения расхождений данных при синхронизации (`data_conflict`). Уведомление генерируется, но отдельного UI для принятия/отклонения нет
- [ ] **Подтверждение смен (UI)** — эндпоинт `PATCH /shifts/:id/confirm` готов, UI в профиле/admin-панели не реализован
- [ ] **Команда из матрицы при подключении** — при привязке `MatrixRegistry` к `StatusRow` автоматически подтягивать `ProjectAssignment` как `ProjectMember` (сейчас команду нужно добавлять вручную)
- [ ] **"Свод матрица" (placeholder)** — вкладка в деталях матрицы; заглушка «Появится после подключения базы цен сотрудников»
- [ ] **Страница "Проекты" (`ProjectsPage`)** — список всех внутренних матриц с их микропроектами; клик → открывает RegistryDetailModal. Приоритет низкий.

---

## 🟡 Технический долг

- [ ] **Debug `console.log` в `statusRows.ts`** (строки 254–262) — три строки `[matrix-block]` печатаются при каждом `PATCH /status-rows/:id` с `matrixRegistryId`. Оставлены после отладки.

- [ ] **Drift схемы Prisma** — модели `ProjectMember` и `StatusRow` в `schema.prisma` не содержат столбцов, добавленных через raw SQL миграции:
  - `project_members`: `employment_type`, `rate_plan`, `rate_fact`, `is_approved`, `field_approvals`, `group_name`
  - `status_rows`: `field_approvals JSONB`, `group_schedule JSONB`
  
  Из-за этого Prisma-клиент не типизирует эти поля, весь CRUD идёт через `$queryRawUnsafe`. Нет type safety, intellisense не работает.  
  **Действие:** добавить поля в `schema.prisma` и регенерировать клиент (пока без перезапуска API через `--skip-generate`).

- [ ] **Удалить неиспользуемые зависимости из `apps/web`** — 5 пакетов установлены, но нигде не импортируются:
  - `@tanstack/react-router` — навигация через `useState<Page>` в `AppShell.tsx`
  - `react-hook-form` — все формы на `useState`
  - `clsx` — нет CSS-классов, только inline styles
  - `@fullcalendar/interaction` — плагин удалён из `CalendarPage.tsx`, из `package.json` не убрали
  - `@fullcalendar/timegrid` — не используется (только `daygrid`)
  
  Команда: `pnpm --filter @tv-shifts/web remove @tanstack/react-router react-hook-form clsx @fullcalendar/interaction @fullcalendar/timegrid`

- [ ] **`setTimeout` без cleanup в `DatabasePage.tsx`** — мутации ставят `setTimeout(() => setState(false), 2000)` без очистки при анмаунте. При быстром переключении страниц возможны stale-обновления состояния.

---

## 🧪 Тесты

- [ ] **E2E через Playwright**: login → открыть Calendar → убедиться что проекты отображаются
- [ ] **Тесты для `InternalShiftsPanel`** — критичная бизнес-логика (группы, расписание, копирование блоков) не покрыта

---

## ⚪ Пожелания

- [ ] **Пагинация проектов** — `GET /status-rows` без лимита; при большом числе строк возможно замедление
- [ ] **Разбивка God-файлов** — `SyncDataPage.tsx` (3854 строк) и `InternalShiftsPanel.tsx` (1979 строк) сложно поддерживать; разбить на субкомпоненты по функциональным областям
- [ ] **Диапазон дат у проекта** — проект может охватывать несколько дней, сейчас только одна дата или список `ProjectDay`
- [ ] **Цветовая маркировка полей при создании** — подсветка неутверждённых полей в форме создания проекта
- [ ] **Битрикс интеграция** — отправка вызывного листа / статуса смены в Битрикс24. Требует предварительного изучения API и выбора точки интеграции (webhook vs OAuth). Начать с PoC.

---

> Текущий счёт тестов: **172 теста, 0 провалов** (`pnpm test`).
