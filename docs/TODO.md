# TODO — TV Shifts

Актуальный список задач по разработке. Что уже готово — в [DONE.md](DONE.md).

---

## Этап 3 — Google Sheets синхронизация

### 3.1 Инфраструктура
- [ ] Настроить Google Service Account, добавить ключ в `.env`
- [ ] Создать клиент Google Sheets API с rate limiting (`apps/api/src/services/sheetsClient.ts`)
- [ ] Утилита для извлечения spreadsheet ID из URL

### 3.2 Парсер таблицы проектов (`GOOGLE_PROJECTS_SHEET_ID`)
- [ ] Читать колонки A–I + «Ссылка на матрицу»
- [ ] Читать цвет ячеек → определять `uncertain_fields`
- [ ] Определять статус: `preliminary` / `ready`
- [ ] Upsert проектов в БД по ключу (client + date или sheet row index)

### 3.3 Парсер реестра матриц (`GOOGLE_REGISTRY_SHEET_ID`)
- [ ] Читать колонки A–L реестра
- [ ] Извлекать ссылки на матрицы из колонки B
- [ ] Upsert записей `matrix_registry` в БД

### 3.4 Парсер матрицы проекта
- [ ] Читать лист СВОД → получать дату проекта и ID
- [ ] Читать строку 2 листа `₽ СМЕНЫ` → массив дат
- [ ] Читать строки 4+ → ФИО, роль, тип занятости, маркеры дней
- [ ] Матчить ФИО → `user_id` (точное совпадение)
- [ ] Upsert `project_assignments` и `shift_entries`

### 3.5 Планировщик и API
- [ ] node-cron: запускать полный цикл каждые 30 минут
- [ ] Реализовать `POST /sync/trigger` (сейчас заглушка)
- [ ] Реализовать `GET /sync/logs` (сейчас заглушка)
- [ ] Запись в `sync_logs` — статус, количество изменений, ошибки

### 3.6 Edge cases
- [ ] Проект есть, матрицы нет → уведомление `no_matrix`
- [ ] ФИО не найдено в users → уведомление `unmatched_name`
- [ ] Колонка B реестра — текст, не URL → пропустить, залогировать

---

## Этап 4 — Учёт смен (Web)

> API полностью готов (`apps/api/src/routes/shifts.ts`).

- [ ] Страница сотрудника: личный календарь смен за месяц
- [ ] Счётчики: итого смен / порог / переработка / отпуск
- [ ] Лог изменений расписания в профиле
- [ ] Подсветка конфликтов на `CalendarPage` (GET `/projects/conflicts`)
- [ ] Карточка конфликта с возможностью переназначить сотрудника
- [ ] Diff UI: панель разрешения конфликтов данных при синхронизации (`data_conflict`)

---

## Этап 5 — Уведомления (Web) + Change Log

> Notifications API готов. Change log — схема есть, middleware нет.

- [ ] Колокольчик в шапке (`AppShell`) с бейджем непрочитанных (`GET /notifications/count`)
- [ ] Дроп-даун с группировкой уведомлений по типу
- [ ] Middleware/хук Prisma: записывать изменения в `change_logs` при update
- [ ] `GET /change-logs?entityType=&entityId=` — эндпоинт истории
- [ ] Лог в карточке проекта и в профиле сотрудника
- [ ] Глобальный лог с фильтрами в admin-панели

---

## Этап 7 — Аналитика

- [ ] `GET /analytics/shifts` — смены по пользователям за период
- [ ] `GET /analytics/projects` — проекты с составом команды
- [ ] Дашборд администратора:
  - Таблица смен по сотрудникам за месяц
  - Переработки / отпуска
  - Кто на каких проектах участвовал
  - Выполненные задачи
- [ ] Фильтры: период, сотрудник, проект

---

## Этап 8 — Деплой

- [ ] `docker-compose.prod.yml` — production конфигурация
- [ ] Nginx reverse proxy + SSL (Let's Encrypt)
- [ ] Настройка домена
- [ ] Переменные окружения для production
- [ ] Бэкапы PostgreSQL (pg_dump по крону)
- [ ] Мониторинг — `/health` уже есть, настроить алерты
