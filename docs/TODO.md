# TODO — TV Shifts

Актуальный список задач по разработке. Что уже готово — в [DONE.md](DONE.md).

---

## 🔴 Баги

- [ ] **`GET /sync/logs` недоступен продюсеру** — роут `requireRole('admin')`, но `SyncButton` отображается и для продюсеров (`isAdmin || isProducer`). При запуске синхронизации продюсером компонент получает 403 при опросе прогресса и не может отображать статус. Исправить: добавить `'producer'` в `requireRole` на этом роуте.

- [ ] **`PATCH /notifications/read-all` затирает глобальные уведомления для всех** — запрос помечает прочитанными и `userId = me.id`, и `userId = null` (глобальные). Любой пользователь сбрасывает счётчик глобальных уведомлений у всех остальных. Обсудить: нужно ли разделить «прочитано мной» от «прочитано глобально».

---

## 🚧 В разработке (заглушки)

- [ ] **Вкладка "Задачи"** — `TasksPage.tsx` — бэкенд полностью готов (`/tasks/*`), нужен UI
- [ ] **Вкладка "Аналитика"** — `AnalyticsPage.tsx` — бэкенд полностью готов (`/analytics/*`), нужен UI
- [ ] **Вкладка "Профиль"** — `ProfilePage.tsx` — бэкенд готов (`/users/:id`, `/shifts/monthly-summary`), нужен UI

---

## 📋 Функционал

- [ ] **Подтверждение смен** — UI для администратора (`PATCH /shifts/:id/confirm`)
- [ ] **Diff UI конфликтов** — панель разрешения расхождений данных при синхронизации (`data_conflict`)
- [ ] **Страница Сотрудники** — `UsersPage.tsx` — добавить отображение `isStaff`, `tabNumber` после рефакторинга

---

## 🟡 Технический долг

- [ ] **Debug `console.log` в syncService.ts (строки ~229–231)** — выводит содержимое каждой строки таблицы проектов при каждом синке. Замусоривает логи в продакшне. Удалить или заменить на `app.log.debug`.

- [ ] **Мёртвый экспорт `isSyncRunning()`** — функция экспортируется из `syncService.ts`, но нигде не импортируется. Удалить.

- [ ] **`any`-касты в роутах** — `formatDeal` в `deals.ts` принимает `any`, `data: any` в `shifts.ts` и `users.ts`, `status as any` в `tasks.ts`. Заменить на нормальные типы или хотя бы `unknown` с проверкой.

- [ ] **`docs/04-database-schema.md`** — описывает старую схему (`projects`, статусы `preliminary/ready/completed`). Обновлена до актуальной `status_rows` — нужна проверка полноты после изменений.

- [ ] **`docs/05-architecture.md`** — перечисляет TanStack Router, shadcn/ui, Tailwind, которых в проекте нет. Обновлена — нужна проверка.

---

## 💡 Улучшения (низкий приоритет)

- [ ] **Пагинация проектов** — сейчас `GET /status-rows` без лимита, при большом количестве строк будет медленно
- [ ] **Фильтр `?dateNull=true` на бэке** — сейчас "Без даты" подгружает все проекты и фильтрует на фронте
- [ ] **Rate limiting на `/auth/login`** — защита от брутфорса (`@fastify/rate-limit`)
- [ ] **Тесты** — в проекте нет ни одного теста. Критические пути для покрытия в первую очередь: `syncService.ts` (парсинг матриц, статусов, цветов), auth routes (login/refresh/logout), роль-доступ в preHandlers.
