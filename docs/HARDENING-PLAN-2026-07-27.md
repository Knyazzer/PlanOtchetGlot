# План ремонтных работ Nexus (pre-development hardening) — 2026-07-27

> Что закрыть ДО добавления новой функциональности: техдолги, безопасность, стандарты, рефакторинг. Вытекает из [AUDIT-2026-07-27.md](AUDIT-2026-07-27.md). Большими мазками; каждый блок при старте раскрывается в детальный план (spec/plan).
>
> Легенда исполнителя: **[код]** — делаю на ветке под ревью; **[Влад/инфра]** — нужно решение/прод-доступ/координация; **[коорд]** — пересекается с идущим WIP, согласовать.

## Phase 0 — Стабилизация рабочего дерева (перед всем)

- [ ] **Разобрать незакоммиченный WIP на `knyazzer`** [Влад/коорд] — в дереве крупная незавершённая работа (форма роли/должности в Персонале: `pages/personnel/`, `users.ts`, `access.ts`, `PersonnelPage.tsx`, `RBAC-MODEL.md`, `seed.ts`, справочники/`refs`, логотипы, `STRATEGIC-GOALS.md`). Пока она не закоммичена/не убрана — чистого дерева для рефакторинга нет и коммиты будут путаться. Решить: закоммитить силами того чата, отложить в stash, или домержить.

## Phase 1 — Безопасность (критично, первым)

- [ ] **Ротация скомпрометированных секретов** [Влад/инфра] — `JWT_SECRET` и пароль БД засветились в git-истории; `JWT_SECRET` общий с Supabase и Inventory. Ротация инвалидирует все сессии, нужна координация с Inventory (пересбор anon-key) по `SECRET-ROTATION.md`. Я готовлю пошаговый runbook, выполняешь ты.
- [ ] **Fail-fast без `JWT_SECRET`** [код] — убрать fail-open дефолт `?? 'dev-secret…'`; в проде отсутствие секрета = отказ старта, не тихий проход.
- [ ] **`@fastify/helmet`** [код] — подключить security-заголовки.
- [ ] **Rate-limit на `/auth/*`** [код] — `@fastify/rate-limit` уже в deps, но не подключён к аутентификации (брутфорс).
- [ ] **CORS** [код] — убрать LAN-CORS в проде, ограничить origin белым списком.
- [ ] **Контейнеры не от root** [код/инфра] — non-root user в Dockerfile.
- [ ] **Field-level RBAC на финансы WI** [код] — скрыть `budget`/`expenses` от не-финансистов (спека предписывает, реализация не подтверждена — вероятная утечка полей).

## Phase 2 — RBAC-фундамент

- [ ] **Честный `requireRole`** [код/коорд] — сейчас проверяет только `admin`, прочие роли молча пропускают всех. Доработать плагин auth под producer/freelancer/head/director (поля в БД есть), затем расставить реальные гарды. Пересекается с идущей формой роли/должности (Phase 0) — согласовать, чтобы не разъехалось.

## Phase 3 — Тесты (страховка перед рефакторингом)

- [ ] **Восстановить критическое покрытие** [код] — инфраструктура (vitest/MSW/тест-БД :5434) готова, реальных тестов ~2. Порядок: auth-плагин → users lifecycle → chats advisory-locks → events → projects/work-items → web-гейты (`App.tsx`). Цель — smoke на критичных путях, чтобы дальнейший рефакторинг был безопасен. Правило проекта: новые фичи — снова с тестами.

## Phase 4 — Функциональные баги и консистентность

- [ ] **events.ts: синк задач-спутников** [код] — при PATCH участников добавленный не получает задачу, удалённый не теряет.
- [ ] **Выровнять права DELETE** work-items vs projects [код].
- [ ] **Данные пользователей** [код] — unique-констрейнт на `tabNumber`, устранить коллизию `generateEmail` у тёзок, дедуп `genTempPassword`/`PW_CHARS`, убрать обход lifecycle через `isActive` в `PATCH /users/:id`.

## Phase 5 — Стандарты и гигиена кода

- [ ] **Линтер на `apps/api`** [код] — сейчас только web; выровнять правила бэкенда.
- [ ] **Синхронизация nginx allow-list ↔ server.ts** [код] — авто-проверка/тест, что каждый префикс роута присутствует в `nginx.conf` (чтобы постмортем 2026-06-15 не повторился).
- [ ] **Удалить мёртвый код и неиспользуемые deps** [код] — `bcryptjs`, `node-cron`, `prom-client`, `@fullcalendar/*`; `Track.clientName/projectName/type` (vestigial); `NavItem.adminOnly`; рудимент импорта Google Sheets (после Phase 7).
- [ ] **DESIGN.md — один источник правды** [код] — свести к `kit.css`, историческое тело вынести в явно помеченный архив-раздел (чтобы не копировали устаревшие Urbanist/оранжевые токены).

## Phase 6 — Рефакторинг god-файлов фронта (под прикрытием тестов)

- [ ] **Раздробить god-файлы** [код] — TasksPage (1621), ChatsPage (1375), ProjectsPage (1170), ProjectCardPage (1119), OrgChart (1091), CalendarPage (1087) на фокусные модули/компоненты. Делать после Phase 3 (тесты как страховка), по одному файлу, с визуальной сверкой.

## Phase 7 — Рудименты и данные (по отмашке / инфра)

- [ ] **Отключить импорт Google Sheets** [Влад] — сейчас единственный канал клиентов/структуры/КФПД; заменить ручным заведением, затем удалить код.
- [ ] **Донор cutover, Этап 4** [Влад/инфра] — донор сменил формат (`pr_v2_*` исчез); закрыть `/api.php` секретом, выполнить гибридный cutover.
- [ ] **Переезд домена** `knzteam.ru → megapolis.media`, Этап 3 роадмапа интеграции [Влад/инфра].

## После hardening — новая функциональность (не входит в этот план)

Три эталон-гэпа из аудита §7: **уведомления (двухфазная WS+Web Push)**, **Web Push per-app VAPID**, **deep-F5 persist**; миграция Nexus на единый ui-kit (схлопнуть дубли); реализация **стратегических целей** (спека-черновик ждёт ревью).

## Предлагаемый порядок исполнения

Phase 0 (разблокировать дерево) → Phase 1 код-часть безопасности (быстро, безопасно, высокая ценность) → Phase 3 тесты критичных путей → Phase 2 RBAC → Phase 4 баги → Phase 5 гигиена → Phase 6 рефакторинг. Инфра/Влад-пункты (ротация секретов, домен, донор, Sheets) — параллельно по мере готовности.
