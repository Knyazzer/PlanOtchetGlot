# Nexus — мастер-план работ (консолидированный)

> Единый список «что делаем по Nexus». Собран 2026-08-04 из спек/роадмапов сессии. Всё локально в ветке `knyazzer`, **не пушим**. Статусы: ✅ сделано · 🔜 ближайшее · ⏳ позже · 🧭 нужен брейншторм/решение.
> Детальные доки: адаптация ui-kit — `plans/2026-08-02-calendar-and-visual-roadmap.md`; календарь-механики — `specs/2026-08-02-calendar-mechanics-decisions.md`; кабинет — `specs/2026-08-02-employee-cabinet-solution.md` (+ аудит `-audit.md`).

## ✅ Сделано в этой сессии (для контекста)
- **ui-kit AppShell**: parity меню/профиля 1:1 с китом (scoped preflight), бренд «Нексус» кириллицей, лого продуктов в nav (`iconSrc`), слот уведомлений в шапке (`headerActions`), порядок блоков меню (Платформа/Внутренние/Внешние).
- **Единые шапки страниц (HeaderPortal)**: контролы Команда/Задачи/Аналитика/Календарь ушли в китовую шапку; **Свод слит в Аналитику** внутренней вкладкой.
- **Календарь date/time**: чипы Дата/Время → мини-модал сбоку; `DatePicker`/`TimePicker`/`ClockDial` (полдень сверху, рабочие часы, **тумблер 12/24**, дефолт 12ч); блюр убран со всех оверлеев; поповер «Календари» + поиск/выбрать-все.
- **Кабинет**: «Мой кабинет» = рабочий стол (виджет статуса MVP, план дня, задачи/дедлайны/события); **3 вкладки [Обзор·Задачи·Треки]**; быстрые действия из шапки убраны.
- **Пульс** (бывш. «Главная»): новостная лента (модель `Post` + API `/posts` + право `adm.news`) + правый рельс (кто работает / дедлайны / глобальные задачи); сэмпл-сид.
- **БД/инфра**: dev пересоздана `reset` (чистая история миграций) + **сэмпл-сид** (5 департаментов, 9 сотрудников, гранты) — dev поднимается наполненным; `CLAUDE.md` актуализирован.

---

## A. Кабинет сотрудника — 🔜 текущий фокус
- 🔜 **Месячная сводка справа в «Мой кабинет» + дате-зависимый кабинет.** Вертикальный столбец месяца (кубик = тип дня); клик по дню → рабочий стол показывает задачи/план на эту дату (в т.ч. **планирование на будущее**). Объединяет план-отчёт (как в TVonly). *(revision в spec кабинета)*
- 🧭 **График работы сотрудника (HR).** Новая модель + HR-конфиг: HR при найме задаёт график (5/2, офис/удалёнка, часы) → **тип дня по умолчанию**. Сотрудник отмечает **начало/конец дня**; self-смена только **больничный/отпуск**; остальное — руководитель; **командировка — из проекта**. → переделать MVP-виджет «Мой статус» (сейчас свободный выбор — заглушка).
- ⏳ **Единый статус/присутствие** (после графика) — свести с `dayFormat`/Сводом аккуратно (несущая логика отчёта).

## B. Пульс — ⏳ доработать
> Редизайн сделан: шапка убрана, новости в формате чата (без автора, писать по праву `adm.news`), «кто работает» с поиском/фильтром/детализацией+«написать в чат», «стратегические задачи отдела» (каркас).
- ⏳ **Новости — фото/вложения** (нужна загрузка файлов + поле media на `Post`).
- ⏳ «Кто работает» — **реальное присутствие** (после A: график/статусы), сейчас превью; детализация рабочего дня в поповере — из реальных данных.
- 🧭 **Стратегические задачи отдела** — новая **модель целей отдела** (per-department), редактирование руководителем; сейчас каркас-заглушка.
- ⏳ Лёгкая аналитика/виджеты (опц.).

## C. Календарь — механики (spec `calendar-mechanics-decisions`) 🧭/⏳
- 🔜 **Overlap параллельных задач + drag-интерактив** (§5–6): равные колонки (неделя ~3, день до ~20), создание параллельной в зазоре справа; **живой пунктир + карточка сбоку без затемнения**, двусторонняя связь, перенос силуэтом с подсветкой слота. *Чистый фронт, без схемы.*
- ⏳ **Задачи опционально в сетке времени** — время (старт–конец) как опц. параметр любой задачи (Task); задано → блок в дне/неделе.
- ⏳ **HR-статусы/отсутствия** — полоса сверху + многодневность одной записью `CalendarEntry.endDate`; удалёнка = режим присутствия (без полосы).
- ⏳ **Заявка на отпуск** — из кабинета → согласование руководителем; больничный сам; **скачивание заявления** (PDF-форма).
- 🧭 **Иерархия календарей** — личный / общий компании / проектный (видишь «свои» мероприятия) / календарь встреч / HR-статусы / **локации (Знаменки) — бронь только через проект** + лёгкий «бронь/внутренний проект».
- ⏳ **Агрегация helicopter-view** — руководители/директора видят подчинённых (параллельный трек).

## D. Треки — 🔜/⏳ (визуал + механики)
- 🔜 **Визуал под канон** (этап 3): причесать Задачи/Треки (кнопки/переключения/отступы), не редизайн.
- ⏳ **Встречи внутри трека** — видеть намеченные по треку встречи в самом треке (не только в календаре).
- ⏳ **Трек = чат** — при создании трека авто-создаётся чат с названием трека, участники автоподключаются (как в megapolis-kanban).

## E. Чаты → канон — 🧭 (этап 1, редизайн)
- 🧭 Привести к канону **Messenger из megapolis-kanban** (самый полный: меню, мини-аккаунты, мобильный, реакции/ответы). Бэкенд **дореализовать** (Nexus-чат старый). Support-чат — **сквозной на компанию**, уведомления только техподдержке. Realtime (WS/typing) — **проверить** (вроде есть).

## F. Гигиена / техдолг — ⏳
- ⏳ `SettingsPage`: подключить `FormatsTab` (написан, не подключён); «Бэкапы» — заглушка.
- ⏳ `project-card/StructureTab` — 3 блока-заглушки.
- ⏳ Панель профиля: «Изменить пароль — скоро» (disabled) — включить (на PersonalCabinet смена работает).
- ⏳ Гейт `canAccessPlatform` — **оставили** (бета-выдача), не трогаем.
- ⏳ Мелкие «грешки» Влада по визуалу (копятся).

## G. Прод-деплой — ⏳ (когда решим)
- Проверить, что у **прод-БД** история миграций консистентна (dev была P3005 — пересоздана; прод отдельный).
- Задеплоить накопленный **hardening** (аудит 2026-07-27, пока только локально) + новые фичи; **ротация секретов** — по `SECRET-ROTATION.md`.
- Три эталон-гэпа (двухфазные уведомления/WebPush/deep-F5) — в ядро (отдельный трек).

---

## Рекомендуемый порядок ближайшего
1. **A: месячная сводка + дате-зависимый кабинет** (фронт, высокий эффект) → 2. **C: overlap+drag календаря** (фронт «мясо») → 3. **A: график HR + статусы** (модель) → 4. Треки визуал + трек-чат → 5. Чаты канон.
