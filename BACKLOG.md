# Backlog

> Актуализирован 2026-08-28 (сверка с кодом). Удалены сделанные, узкие пункты слиты в эпики.
> Сделано и убрано: заявки отпуск/больничный→согласование→docx (routes/requests), розетки Task (source/externalId/externalUrl, service-account, вычисляемый Свод), персонал таблица+карточка, события со временем блоком в календаре, консистентность миграций прод-БД (проверена на клоне).

## Продуктовые модули

- [ ] HR-модуль (эпик): полный функционал HR-департамента поверх заявок/статусов — мониторинг персонала (кто в отпуске/больничном/отгуле, даты, кто и что запросил); учёт больничных (кол-во, справка принесена/нет) и отгулов; **свод по ФОТ** в конце месяца; статистика по сотрудникам. Осталось поверх готового: presence/requests/day-status уже есть; свести profile.status в единую модель; dev-инструмент «переоткрыть/сбросить день» для отладки цикла
- [ ] Проекты (эпик): аудит и дореализация проектной логики — кто заводит проект и как, права редактирования, состав, дерево WorkItem и ветвления; дизайн StructureTab/project-card (что показывать). Базовые CRUD есть — нужен ревью+достройка
- [ ] Чаты: дореализовать бэкенд-механики — **реакции/ответы** (сейчас заготовки схемы без эндпоинтов), привести к канону Messenger из megapolis-kanban, support-чат сквозной на компанию, визуальная адаптация под стандарт. (self-заметки/избранное/pinned уже есть)
- [ ] Стратегия (аудит): проверить полноту реализации вкладки (канбан/детали/история/привязка треков есть — ревью «заявлено vs реализовано»)
- [ ] Календарь: многодневные HR-статусы полосой сверху ОТ–ДО (нужно поле `CalendarEntry.endDate` — сейчас нет, статусы рисуются поденными чипами)
- [ ] Календарь: helicopter-view — руководители/директора видят календари подчинённых (у day-entries орг-охват есть, календарь его не использует)
- [ ] Календарь: метка «трековых» встреч в сетке (`Event.trackId` есть в схеме, маппинг в CalEvent не переносит)
- [ ] Календарь: иерархия календарей (личный/компании/проектный/встреч/HR) + бронь локаций Знаменки только через проект (с проверкой конфликтов)

## Платформа / UX

- [ ] Уведомления (эпик): агрегатор `/notifications` + колокол есть (опрос 30с) → добавить **Web Push + Badging + WS** для не-чатовых событий; аудит реализованного + внутренние (новое сообщение, назначили/сменили статус задачи, реакции)
- [ ] PWA / приложение: внедрение — Web Push, диплинки/protocol handler, deep-F5 (сейчас persist page + history частично); **PWA-only на мобиле** — баннер «установить приложение» + запрет пользования в мобильном браузере
- [ ] Мобильная вёрстка (эпик): переработать меню и каждую функциональную вкладку под телефон (`useIsMobile` частичный, поэкранной вёрстки нет)
- [ ] Адаптив desktop: при сужении окна блоки Обзора/кабинета скрываются в НЕправильном порядке; открытие правой панели чатов ломает вёрстку даже на 16:9. Breakpoints + порядок сворачивания + резерв места под панель чата
- [ ] Ролевая модель / RBAC (эпик + баг): глобальный аудит «кто что может», настройка мастер-админом, визуализация модели. **БАГ (подтверждён):** фантомные пункты меню `adm.news` («Публикация в Пульс») и `prod.board` («Доска производства») лезут в навигацию из реестра модулей (`AppShell.tsx` фильтр extraModules), клик → несуществующая страница — убрать. Наполнить `roles` producer/freelancer из оргструктуры (плагин `requireRole` уже fail-closed — CLAUDE.md L111 про «пропускает любого» устарел)
- [ ] UI-консистентность: убрать нативные `window.confirm`/`alert` (DashboardPage/TracksPage/PersonnelPage/ListsPage/OrgChart/SettingsPage/DayModal/ProjectDetail/WorkScheduleEditor) → `ConfirmModal`/тост в стиле (правило попапов, без блюра); ошибки API — тост/inline, не браузерный alert. Централизованные токены светлой темы; доводка ui-kit

## Данные / надёжность

- [ ] **Period-Lock — эпик «Фиксация периодов» + Надсмотрщик** (server-side): блокировка прошлых недель/месяца (роллинг grace, жёсткий замок в конце месяца), класс уведомлений-напоминаний с read-tracking, блок «Требуется действие» (зелёный/красный), полноэкранный модал фиксации, просрочка задач + история переноса. Спека — `docs/PERIOD-LOCK-2026-08-29.md` (открыто §8: override, календарный край недели). **ПОГЛОЩАЕТ** клиентские guard'ы закрытия дня/переноса задач → перенести на сервер ([[server-side-enforcement-never-trust-client]])
- [ ] HR-надёжность: аудит-лог смены `day_format` (нет модели DayEntryLog — истории нет); захардить отпуск-сироту (день выводить из активных заявок ИЛИ идемпотентная сверка/чистка). `unreflectLeave` на revoke уже есть
- [ ] Пульс: новости — фото/вложения (сейчас `posts` только текст; нужен upload/media)
- [ ] Экосистема (нужно решение, отдельное направление): Nexus = слой сведения — внешние продукты пушат задачи/уведомления по сервис-контракту, Nexus зеркалит read-only, правка → deep-link. Розетки Task (source/externalId/externalUrl) уже заложены
- [ ] Инфра/техдолг: задеплоить накопленный hardening (аудит 2026-07-27) + ротация секретов; **Фаза 3.5 в деплой-ранбуке** — прогон приложения на клоне прод-БД по затронутым путям (чтобы не было серии пост-деплойных багов, как в релизе 2026-08-27); восстановить тестовое покрытие; актуализировать `docs/DONE.md` (обрывается на 2026-06-11)
